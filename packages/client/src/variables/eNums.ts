export enum EAuthType {
  text = 'text',
  password = 'password',
  rePassword = 'rePassword',
  username = 'username',
  email = 'email',
  submit = 'submit',
}

export enum ETableSort {
  asc = 'asc',
  desc = 'desc',
}
